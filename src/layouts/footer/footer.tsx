import { Eyebrow } from "../../components/components";

export const Footer = () => {
  return (
    <section className = "footer">
      <Eyebrow text = "TE AMO MUCHO AMORCITO" accent/>
    </section>
  );
};